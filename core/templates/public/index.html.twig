{% extends 'public/base.html.twig' %}

{% block title %}Welcome · Easy-Wi NextGen{% endblock %}

{% block content %}
<div class="space-y-8">
    <div class="rounded-2xl bg-gradient-to-br from-indigo-600 via-indigo-500 to-slate-900 px-8 py-10 text-white shadow-lg">
        <p class="text-sm uppercase tracking-[0.3em] text-indigo-200">Welcome to {{ siteName }}</p>
        <h1 class="mt-4 text-3xl font-semibold sm:text-4xl">Your hosting portal starts here.</h1>
        <p class="mt-3 max-w-2xl text-sm text-indigo-100">Explore public service updates, browse available servers, or sign in to manage your account.</p>
        <div class="mt-6 flex flex-wrap gap-3">
            <a href="/login" class="inline-flex items-center rounded-lg bg-white px-4 py-2 text-sm font-semibold text-indigo-700">Login</a>
            <a href="/register" class="inline-flex items-center rounded-lg border border-indigo-200/60 px-4 py-2 text-sm font-semibold text-white">Register</a>
            <a href="/status" class="inline-flex items-center rounded-lg border border-indigo-200/60 px-4 py-2 text-sm font-semibold text-white">View status</a>
        </div>
    </div>

    <div class="grid gap-4 md:grid-cols-2 xl:grid-cols-3">
        <div class="rounded-xl border border-slate-200 bg-white p-6">
            <h2 class="text-base font-semibold text-slate-900">Status overview</h2>
            <p class="mt-2 text-sm text-slate-600">Check system uptime, maintenance windows, and incident reports.</p>
            <a href="/status" class="mt-4 inline-flex text-sm font-semibold text-indigo-600">Go to status →</a>
        </div>
        <div class="rounded-xl border border-slate-200 bg-white p-6">
            <h2 class="text-base font-semibold text-slate-900">Server directory</h2>
            <p class="mt-2 text-sm text-slate-600">Browse featured public servers or find new experiences.</p>
            <a href="/servers" class="mt-4 inline-flex text-sm font-semibold text-indigo-600">Browse servers →</a>
        </div>
        <div class="rounded-xl border border-slate-200 bg-white p-6">
            <h2 class="text-base font-semibold text-slate-900">Downloads &amp; docs</h2>
            <p class="mt-2 text-sm text-slate-600">Get launchers, configuration files, and documentation.</p>
            <a href="/downloads" class="mt-4 inline-flex text-sm font-semibold text-indigo-600">View downloads →</a>
        </div>
    </div>
</div>
{% endblock %}
