<div class="border-b border-slate-200 px-6 py-4">
    <h3 class="text-lg font-semibold">Reply</h3>
    <p class="text-sm text-slate-500">Send an update to the customer.</p>
</div>
<form class="p-6 space-y-4" method="post" action="/admin/tickets/{{ ticket.id }}/messages" hx-post="/admin/tickets/{{ ticket.id }}/messages" hx-target="#ticket-messages" hx-swap="outerHTML">
    <div>
        <label class="text-xs font-semibold uppercase text-slate-500">Message</label>
        <textarea name="message" rows="6" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" placeholder="Write a response..."></textarea>
    </div>
    <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-slate-900 text-white">Send Reply</button>
</form>
