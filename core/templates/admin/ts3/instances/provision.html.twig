{% extends 'admin/base.html.twig' %}

{% block title %}{{ t('admin_ts3_provision_title', page_locale()) }} · Easy-Wi NextGen{% endblock %}

{% block content %}
<div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between mb-6">
    <div>
        <h1 class="text-2xl font-semibold">{{ t('admin_ts3_provision_title', page_locale()) }}</h1>
        <p class="text-sm text-slate-500">{{ t('admin_ts3_provision_intro', page_locale()) }}</p>
    </div>
    <a class="text-sm font-medium text-slate-600 hover:text-slate-900" href="/admin/ts3/instances/new">
        {{ t('admin_ts3_provision_back', page_locale()) }}
    </a>
</div>

<div id="ts3-instance-form" class="rounded-xl bg-white border border-slate-200">
    {% include 'admin/ts3/instances/_form.html.twig' with { customers: customers, nodes: nodes, form: form } %}
</div>
{% endblock %}
