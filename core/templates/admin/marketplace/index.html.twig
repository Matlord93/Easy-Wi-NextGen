{% extends 'admin/base.html.twig' %}

{% block title %}Marketplace · Easy-Wi NextGen{% endblock %}

{% block content %}
    <div class="space-y-6">
        <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
            <h1 class="text-xl font-semibold text-slate-900">{{ t('admin_marketplace_title', page_locale()) }}</h1>
            <p class="mt-1 text-sm text-slate-500">{{ t('admin_marketplace_intro', page_locale()) }}</p>
        </div>

        <div class="grid gap-6 lg:grid-cols-2">
            <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
                <div class="flex items-start justify-between">
                    <div>
                        <h2 class="text-lg font-semibold text-slate-900">{{ t('admin_marketplace_storefront_title', page_locale()) }}</h2>
                        <p class="mt-2 text-sm text-slate-500">{{ t('admin_marketplace_storefront_intro', page_locale()) }}</p>
                    </div>
                    <span class="rounded-full bg-indigo-50 px-3 py-1 text-xs font-semibold text-indigo-600">{{ t('admin_marketplace_coming_soon', page_locale()) }}</span>
                </div>
                <ul class="mt-4 space-y-2 text-sm text-slate-600">
                    <li class="flex items-center gap-2">
                        <span class="h-2 w-2 rounded-full bg-indigo-400"></span>
                        Curated partner extensions
                    </li>
                    <li class="flex items-center gap-2">
                        <span class="h-2 w-2 rounded-full bg-indigo-400"></span>
                        Versioned updates and changelogs
                    </li>
                    <li class="flex items-center gap-2">
                        <span class="h-2 w-2 rounded-full bg-indigo-400"></span>
                        Role-aware install controls
                    </li>
                </ul>
            </div>
            <div class="rounded-2xl border border-slate-200 bg-white p-6 shadow-sm">
                <h2 class="text-lg font-semibold text-slate-900">{{ t('admin_marketplace_dev_portal_title', page_locale()) }}</h2>
                <p class="mt-2 text-sm text-slate-500">{{ t('admin_marketplace_dev_portal_intro', page_locale()) }}</p>
                <div class="mt-6 rounded-xl border border-dashed border-slate-200 bg-slate-50 px-4 py-6 text-center text-sm text-slate-400">
                    Marketplace APIs and tooling will appear in a future release.
                </div>
            </div>
        </div>
    </div>
{% endblock %}
