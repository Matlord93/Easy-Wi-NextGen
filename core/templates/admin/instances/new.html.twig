{% extends 'admin/base.html.twig' %}

{% block title %}{{ t('admin_instances_new_title', page_locale()) }} · Easy-Wi NextGen{% endblock %}

{% block content %}
<div class="flex flex-col gap-4 sm:flex-row sm:items-center sm:justify-between mb-6">
    <div>
        <h1 class="text-2xl font-semibold">{{ t('admin_instances_new_title', page_locale()) }}</h1>
        <p class="text-sm text-slate-700">{{ t('admin_instances_new_intro', page_locale()) }}</p>
    </div>
    <a class="text-sm font-medium text-slate-600 hover:text-slate-900" href="/admin/instances">
        {{ t('admin_instances_back', page_locale()) }}
    </a>
</div>

<div class="grid grid-cols-1 gap-6 lg:grid-cols-3">
    <div class="space-y-6 lg:col-span-2">
        <div class="rounded-xl border border-slate-200 bg-white p-6">
            <h2 class="text-lg font-semibold">{{ t('admin_instances_new_overview_title', page_locale()) }}</h2>
            <p class="mt-2 text-sm text-slate-700">{{ t('admin_instances_new_overview_intro', page_locale()) }}</p>
            <ul class="mt-4 space-y-2 text-sm text-slate-600">
                <li>• {{ t('admin_instances_new_overview_step_template', page_locale()) }}</li>
                <li>• {{ t('admin_instances_new_overview_step_customer', page_locale()) }}</li>
                <li>• {{ t('admin_instances_new_overview_step_resources', page_locale()) }}</li>
            </ul>
            <p class="mt-4 text-sm text-slate-700">{{ t('admin_instances_new_overview_note', page_locale()) }}</p>
            <a class="mt-6 inline-flex items-center rounded-lg bg-indigo-600 px-4 py-2 text-sm font-medium text-white" href="/admin/instances/provision">
                {{ t('admin_instances_new_action', page_locale()) }}
            </a>
        </div>
    </div>
</div>
{% endblock %}
