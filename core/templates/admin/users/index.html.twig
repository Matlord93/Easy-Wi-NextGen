{% extends 'admin/base.html.twig' %}

{% block title %}{{ t('user_management_title', page_locale()) }} · Easy-Wi NextGen{% endblock %}

{% block content %}
<div class="flex flex-col gap-6">
    <div>
        <h1 class="text-2xl font-semibold">{{ t('user_management_title', page_locale()) }}</h1>
        <p class="text-sm text-slate-700">{{ t('user_management_description', page_locale()) }}</p>
    </div>

    {% if errors is not empty %}
        <div class="rounded-xl border border-rose-200 bg-rose-50 px-6 py-4 text-sm text-rose-700">
            <ul class="list-disc space-y-1 pl-5">
                {% for error in errors %}
                    <li>{{ error }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}

    {% if success is not empty %}
        <div class="rounded-xl border border-emerald-200 bg-emerald-50 px-6 py-4 text-sm text-emerald-700">
            <ul class="list-disc space-y-1 pl-5">
                {% for message in success %}
                    <li>{{ message }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}

    <div class="rounded-xl border border-slate-200 bg-white p-6 shadow-sm">
        <h2 class="text-lg font-semibold">{{ t('user_management_create_title', page_locale()) }}</h2>
        <p class="text-sm text-slate-700">{{ t('user_management_create_intro', page_locale()) }}</p>
        <form method="post" action="/admin/users" class="mt-4 grid gap-4 md:grid-cols-4">
            <div class="md:col-span-2">
                <label class="text-xs font-semibold uppercase text-slate-700">{{ t('email', page_locale()) }}</label>
                <input type="email" name="email" value="{{ createForm.email }}" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" required>
            </div>
            <div>
                <label class="text-xs font-semibold uppercase text-slate-700">{{ t('type', page_locale()) }}</label>
                <select name="type" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                    <option value="customer" {{ createForm.type == 'customer' ? 'selected' : '' }}>{{ t('user_type_customer', page_locale()) }}</option>
                    <option value="reseller" {{ createForm.type == 'reseller' ? 'selected' : '' }}>{{ t('user_type_reseller', page_locale()) }}</option>
                    <option value="admin" {{ createForm.type == 'admin' ? 'selected' : '' }}>{{ t('user_type_admin', page_locale()) }}</option>
                </select>
            </div>
            <div>
                <label class="text-xs font-semibold uppercase text-slate-700">{{ t('password', page_locale()) }}</label>
                <input type="password" name="password" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" required>
            </div>
            <div class="md:col-span-2">
                <label class="text-xs font-semibold uppercase text-slate-700">{{ t('password_confirm', page_locale()) }}</label>
                <input type="password" name="password_confirm" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm" required>
            </div>
            <div class="md:col-span-2 flex items-end">
                <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">{{ t('user_management_create_action', page_locale()) }}</button>
            </div>
        </form>
    </div>

    {% if canManageSshKeys and pendingSshKeys is not empty %}
        <div class="rounded-xl border border-amber-200 bg-amber-50 p-6 shadow-sm">
            <h2 class="text-lg font-semibold text-amber-900">{{ t('user_management_ssh_pending_title', page_locale()) }}</h2>
            <p class="text-sm text-amber-800">{{ t('user_management_ssh_pending_intro', page_locale()) }}</p>
            <div class="mt-4 space-y-4">
                {% for pending in pendingSshKeys %}
                    <div class="rounded-lg border border-amber-200 bg-white p-4">
                        <div class="flex flex-wrap items-center justify-between gap-3">
                            <div>
                                <p class="text-sm font-semibold text-slate-900">{{ pending.email }}</p>
                                <p class="text-xs text-slate-600">{{ t('user_management_ssh_pending_label', page_locale()) }}</p>
                            </div>
                            <div class="flex items-center gap-2">
                                <form method="post" action="/admin/users/{{ pending.id }}/ssh-key/approve">
                                    <button type="submit" class="rounded-lg bg-emerald-600 px-3 py-1.5 text-xs font-semibold text-white hover:bg-emerald-500">
                                        {{ t('user_management_ssh_pending_approve', page_locale()) }}
                                    </button>
                                </form>
                                <form method="post" action="/admin/users/{{ pending.id }}/ssh-key/reject">
                                    <button type="submit" class="rounded-lg border border-slate-200 px-3 py-1.5 text-xs font-semibold text-slate-700 hover:bg-slate-50">
                                        {{ t('user_management_ssh_pending_reject', page_locale()) }}
                                    </button>
                                </form>
                            </div>
                        </div>
                        <textarea
                            rows="3"
                            class="mt-3 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-xs text-slate-600"
                            readonly
                        >{{ pending.key }}</textarea>
                    </div>
                {% endfor %}
            </div>
        </div>
    {% endif %}

    <div class="rounded-xl border border-slate-200 bg-white">
        <div class="overflow-x-auto">
            <table class="min-w-full divide-y divide-slate-200 text-sm">
                <thead class="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-700">
                    <tr>
                        <th class="px-4 py-3">{{ t('email', page_locale()) }}</th>
                        <th class="px-4 py-3">{{ t('type', page_locale()) }}</th>
                        <th class="px-4 py-3">{{ t('created', page_locale()) }}</th>
                        <th class="px-4 py-3">{{ t('portal_language', page_locale()) }}</th>
                        <th class="px-4 py-3">{{ t('invoice_locale', page_locale()) }}</th>
                        <th class="px-4 py-3"></th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-slate-200">
                    {% for user in users %}
                        <tr class="bg-white">
                            <td class="px-4 py-3 text-sm font-medium text-slate-900">{{ user.email }}</td>
                            <td class="px-4 py-3 text-xs uppercase text-slate-700">{{ user.type }}</td>
                            <td class="px-4 py-3 text-xs text-slate-700">{{ user.created_at|date('Y-m-d H:i') }}</td>
                            <td class="px-4 py-3">
                                <form method="post" action="/admin/users/{{ user.id }}/preferences" class="flex flex-wrap items-center gap-2">
                                    <select name="portal_language" class="rounded-lg border border-slate-200 bg-white px-2 py-1 text-xs">
                                        {% for code, label in portalLanguages %}
                                            <option value="{{ code }}" {{ user.portal_language == code ? 'selected' : '' }}>{{ label }}</option>
                                        {% endfor %}
                                    </select>
                            </td>
                            <td class="px-4 py-3">
                                    <select name="locale" class="rounded-lg border border-slate-200 bg-white px-2 py-1 text-xs">
                                        {% for code, label in locales %}
                                            <option value="{{ code }}" {{ user.locale == code ? 'selected' : '' }}>{{ label }}</option>
                                        {% endfor %}
                                    </select>
                            </td>
                            <td class="px-4 py-3">
                                    <button type="submit" class="rounded-lg bg-indigo-600 px-3 py-1 text-xs font-semibold text-white hover:bg-indigo-500">{{ t('save', page_locale()) }}</button>
                                    {% if not user.has_preferences %}
                                        <span class="ml-2 text-xs text-amber-600">{{ t('defaults_applied', page_locale()) }}</span>
                                    {% endif %}
                                </form>
                            </td>
                        </tr>
                        <tr class="bg-slate-50/40">
                            <td colspan="6" class="px-4 py-4">
                                <form method="post" action="/admin/users/{{ user.id }}/details" class="grid gap-3 md:grid-cols-6">
                                    <div class="md:col-span-2">
                                        <label class="text-xs font-semibold uppercase text-slate-700">{{ t('email', page_locale()) }}</label>
                                        <input type="email" name="email" value="{{ user.email }}" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                                    </div>
                                    <div>
                                        <label class="text-xs font-semibold uppercase text-slate-700">{{ t('type', page_locale()) }}</label>
                                        <select name="type" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                                            <option value="customer" {{ user.type == 'customer' ? 'selected' : '' }}>{{ t('user_type_customer', page_locale()) }}</option>
                                            <option value="reseller" {{ user.type == 'reseller' ? 'selected' : '' }}>{{ t('user_type_reseller', page_locale()) }}</option>
                                            <option value="admin" {{ user.type == 'admin' ? 'selected' : '' }}>{{ t('user_type_admin', page_locale()) }}</option>
                                        </select>
                                    </div>
                                    <div>
                                        <label class="text-xs font-semibold uppercase text-slate-700">{{ t('new_password', page_locale()) }}</label>
                                        <input type="password" name="password" placeholder="{{ t('password_optional', page_locale()) }}" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                                    </div>
                                    <div>
                                        <label class="text-xs font-semibold uppercase text-slate-700">{{ t('password_confirm', page_locale()) }}</label>
                                        <input type="password" name="password_confirm" class="mt-2 w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                                    </div>
                                    <div class="flex flex-wrap items-end gap-2">
                                        <button type="submit" class="rounded-lg bg-slate-900 px-4 py-2 text-xs font-semibold text-white">{{ t('user_management_update_action', page_locale()) }}</button>
                                        {% if canImpersonate and user.type == 'customer' %}
                                            <button
                                                type="submit"
                                                formaction="/admin/users/{{ user.id }}/impersonate"
                                                formmethod="post"
                                                formtarget="_blank"
                                                class="rounded-lg bg-indigo-600 px-4 py-2 text-xs font-semibold text-white hover:bg-indigo-500"
                                            >
                                                {{ t('user_management_impersonate_action', page_locale()) }}
                                            </button>
                                        {% endif %}
                                    </div>
                                </form>
                                {% if user.type in ['admin', 'superadmin'] %}
                                    <div class="mt-4 rounded-lg border border-slate-200 bg-white px-4 py-3">
                                        <div class="flex flex-wrap items-center justify-between gap-3">
                                            <div>
                                                <p class="text-xs font-semibold uppercase text-slate-700">{{ t('user_management_ssh_access_label', page_locale()) }}</p>
                                                <p class="text-xs text-slate-600">
                                                    {% if user.ssh_key_set %}
                                                        {{ t('user_management_ssh_key_present', page_locale()) }}
                                                    {% elseif user.ssh_key_pending %}
                                                        {{ t('user_management_ssh_key_pending', page_locale()) }}
                                                    {% else %}
                                                        {{ t('user_management_ssh_key_missing', page_locale()) }}
                                                    {% endif %}
                                                </p>
                                            </div>
                                            {% if canManageSshKeys and user.type == 'admin' %}
                                                <form method="post" action="/admin/users/{{ user.id }}/ssh-access" class="flex items-center gap-2">
                                                    <label class="flex items-center gap-2 text-xs text-slate-700">
                                                        <input type="hidden" name="ssh_key_enabled" value="0">
                                                        <input type="checkbox" name="ssh_key_enabled" value="1" class="h-4 w-4 rounded border-slate-300 text-indigo-600" {{ user.ssh_key_enabled ? 'checked' : '' }}>
                                                        {{ t('user_management_ssh_access_toggle', page_locale()) }}
                                                    </label>
                                                    <button type="submit" class="rounded-lg bg-indigo-600 px-3 py-1 text-xs font-semibold text-white hover:bg-indigo-500">{{ t('save', page_locale()) }}</button>
                                                </form>
                                            {% else %}
                                                <span class="text-xs text-slate-600">
                                                    {{ user.ssh_key_enabled ? t('user_management_ssh_access_enabled', page_locale()) : t('user_management_ssh_access_disabled', page_locale()) }}
                                                </span>
                                            {% endif %}
                                        </div>
                                    </div>
                                {% endif %}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="6" class="px-4 py-6 text-center text-sm text-slate-600">{{ t('no_users_found', page_locale()) }}</td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>
</div>
{% endblock %}
