<div class="px-5 py-4 border-b border-slate-200">
    <h2 class="text-lg font-semibold">Create Template</h2>
    <p class="text-sm text-slate-500">Define immutable build params for admin provisioning.</p>
</div>
<div class="p-5 space-y-4">
    {% if form.errors is not empty %}
        <div class="rounded-lg border border-rose-200 bg-rose-50 px-4 py-3 text-sm text-rose-700">
            <p class="font-semibold mb-1">Please fix the following:</p>
            <ul class="list-disc list-inside space-y-1">
                {% for error in form.errors %}
                    <li>{{ error }}</li>
                {% endfor %}
            </ul>
        </div>
    {% endif %}
    <form
        class="space-y-4"
        hx-post="/admin/templates"
        hx-target="#template-form"
        hx-swap="outerHTML"
    >
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Game Key</label>
            <input name="game_key" value="{{ form.game_key }}" placeholder="cs2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm">
            <p class="mt-1 text-xs text-slate-500">Stable identifier used by agents and automation.</p>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Display Name</label>
            <input name="display_name" value="{{ form.display_name }}" placeholder="Counter-Strike 2 Dedicated Server" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm">
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Description</label>
            <textarea name="description" rows="2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="Short summary for admins">{{ form.description }}</textarea>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Steam App ID</label>
            <input name="steam_app_id" value="{{ form.steam_app_id }}" placeholder="730" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm">
            <p class="mt-1 text-xs text-slate-500">Optional, used for SteamCMD installs.</p>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Sniper Profile</label>
            <input name="sniper_profile" value="{{ form.sniper_profile }}" placeholder="steam" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm">
            <p class="mt-1 text-xs text-slate-500">Optional build profile for the Sniper provider.</p>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Required Ports</label>
            {% set selected_ports = form.required_ports is iterable ? form.required_ports : [] %}
            <div class="mt-2 grid grid-cols-2 gap-2 text-sm text-slate-600">
                {% for port in ['game', 'query', 'rcon', 'tv'] %}
                    <label class="flex items-center gap-2 rounded-lg border border-slate-200 bg-slate-50 px-3 py-2">
                        <input type="checkbox" name="required_ports[]" value="{{ port }}" {% if port in selected_ports %}checked{% endif %} class="rounded border-slate-300 text-indigo-600">
                        <span class="font-medium">{{ port|upper }}</span>
                    </label>
                {% endfor %}
            </div>
            <p class="mt-1 text-xs text-slate-500">Game port is required; other ports are optional.</p>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Fixed Start Params</label>
            <textarea name="start_params" rows="2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="-game cs2 -tickrate 128">{{ form.start_params }}</textarea>
            <p class="mt-1 text-xs text-slate-500">Immutable command args enforced by the agent.</p>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Environment Vars (one per line)</label>
            <textarea name="env_vars" rows="3" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="SERVER_NAME=My Server">{{ form.env_vars }}</textarea>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Config Files (path | description)</label>
            <textarea name="config_files" rows="3" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="game/csgo/cfg/server.cfg | Default server.cfg">{{ form.config_files }}</textarea>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Plugin Paths (one per line)</label>
            <textarea name="plugin_paths" rows="2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="game/csgo/addons/metamod">{{ form.plugin_paths }}</textarea>
        </div>
        <div class="rounded-lg border border-slate-200 bg-slate-50 p-4">
            <div class="flex items-center justify-between">
                <label class="text-xs font-semibold text-slate-500 uppercase">FastDL Settings</label>
                <label class="flex items-center gap-2 text-xs font-medium text-slate-600">
                    <input type="checkbox" name="fastdl_enabled" value="1" {% if form.fastdl_enabled %}checked{% endif %} class="rounded border-slate-300 text-indigo-600">
                    Enabled
                </label>
            </div>
            <div class="mt-3 space-y-2">
                <input name="fastdl_base_url" value="{{ form.fastdl_base_url }}" placeholder="https://fastdl.example.com" class="w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
                <input name="fastdl_root_path" value="{{ form.fastdl_root_path }}" placeholder="/srv/fastdl" class="w-full rounded-lg border border-slate-200 bg-white px-3 py-2 text-sm">
            </div>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Install Command</label>
            <textarea name="install_command" rows="2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="steamcmd +login anonymous +force_install_dir /srv/gs +app_update 740 validate +quit">{{ form.install_command }}</textarea>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Update Command</label>
            <textarea name="update_command" rows="2" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm" placeholder="steamcmd +login anonymous +force_install_dir /srv/gs +app_update 740 +quit">{{ form.update_command }}</textarea>
        </div>
        <div>
            <label class="block text-xs font-semibold text-slate-500 uppercase">Allowed Game Switch Flags (comma separated)</label>
            <input name="allowed_switch_flags" value="{{ form.allowed_switch_flags }}" placeholder="+map,+maxplayers" class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm">
        </div>
        <div class="flex flex-wrap items-center gap-3">
            <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white">Create Template</button>
            <button
                type="button"
                class="rounded-lg border border-slate-200 px-4 py-2 text-sm font-medium text-slate-600"
                hx-get="/admin/templates/form"
                hx-target="#template-form"
                hx-swap="outerHTML"
            >Reset</button>
            <button
                type="button"
                class="rounded-lg border border-indigo-200 bg-indigo-50 px-4 py-2 text-sm font-semibold text-indigo-700"
                hx-post="/admin/templates/preview"
                hx-target="#template-preview"
                hx-include="closest form"
            >Preview & Dry Run</button>
        </div>
    </form>
</div>
