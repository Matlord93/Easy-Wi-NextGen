{% extends 'admin/base.html.twig' %}


{% block content %}
<div class="flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between mb-6">
    <div>
        <a class="text-sm text-slate-700 hover:text-slate-700" href="/admin/templates">{{ t('back', page_locale()) }}</a>
        <h1 class="text-2xl font-semibold">{{ t('admin_templates_import_title', page_locale()) }}</h1>
        <p class="text-sm text-slate-700">{{ t('admin_templates_import_intro', page_locale()) }}</p>
    </div>
    <div class="flex items-center gap-3">
        <a class="rounded-lg bg-indigo-600 px-4 py-2 text-sm font-semibold text-white" href="/admin/templates/new">
            {{ t('admin_templates_create_button', page_locale()) }}
        </a>
    </div>
</div>

{% include 'admin/templates/_import.html.twig' with { import: import } %}
{% endblock %}
