<div
    id="gameserver-settings"
    class="space-y-4"
    data-url-settings="{{ path('customer_instance_settings_api_summary', {id: instance.id}) }}"
    data-url-configs="{{ path('customer_instance_configs_envelope_list', {id: instance.id}) }}"
    data-url-config-show="{{ path('customer_instance_configs_envelope_show', {id: instance.id, configId: '__CONFIG_ID__'}) }}"
    data-url-config-save="{{ path('customer_instance_configs_envelope_update', {id: instance.id, configId: '__CONFIG_ID__'}) }}"
    data-url-config-apply="{{ path('customer_instance_configs_envelope_apply', {id: instance.id, configId: '__CONFIG_ID__'}) }}"
    data-url-config-create="{{ path('customer_instance_configs_envelope_create', {id: instance.id}) }}"
    data-url-slots="{{ path('customer_instance_slots_update', {id: instance.id}) }}"
    data-url-health="{{ path('customer_instance_settings_api_health', {id: instance.id}) }}"
    data-url-automation="{{ path('customer_instance_settings_api_automation_update', {id: instance.id}) }}"
>
    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <div class="flex items-center justify-between gap-2">
            <h3 class="text-sm font-semibold text-slate-100">{{ t('customer_instance_tab_settings', page_locale()) }}</h3>
            <p id="gs-settings-meta" class="text-xs text-slate-300">Loading settings…</p>
        </div>
        <div id="gs-settings-error" class="hidden mt-3 rounded-lg border border-rose-300 bg-rose-50 p-3 text-sm text-rose-700"></div>
    </div>

    <div class="grid gap-4 lg:grid-cols-3">
        <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
            <div class="flex items-center justify-between gap-2">
                <h4 class="text-xs font-semibold uppercase text-slate-300">Configs</h4>
                <button type="button" id="gs-settings-create" class="ui-button ui-button--ghost">Create</button>
            </div>
            <div class="mt-3 space-y-2">
                <input id="gs-settings-config-search" type="search" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-3 py-2 text-xs text-slate-100" placeholder="Search configs">
                <select id="gs-settings-config-scope" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-3 py-2 text-xs text-slate-100">
                    <option value="">All scopes</option>
                    <option value="template">Template</option>
                    <option value="instance">Instance</option>
                </select>
            </div>
            <ul id="gs-settings-config-list" class="mt-3 space-y-2">
                <li class="text-sm text-slate-400">Loading configs…</li>
            </ul>
        </div>
        <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4 lg:col-span-2">
            <h4 class="text-xs font-semibold uppercase text-slate-300">Editor</h4>
            <p id="gs-settings-selected" class="mt-2 text-xs text-slate-400">Select a config to edit</p>
            <textarea id="gs-settings-content" class="mt-3 h-64 w-full rounded-lg border border-slate-600 bg-slate-900 p-3 font-mono text-xs text-slate-100" placeholder="Select config to edit"></textarea>
            <div class="mt-3 flex flex-wrap gap-2">
                <button type="button" id="gs-settings-save" class="ui-button ui-button--primary">Save</button>
                <button type="button" id="gs-settings-apply" class="ui-button ui-button--ghost">Apply</button>
            </div>
        </div>
    </div>

    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <h4 class="text-xs font-semibold uppercase text-slate-300">Slots</h4>
        <div class="mt-3 flex items-center gap-2">
            <input id="gs-settings-slots" type="number" min="1" class="w-40 rounded-lg border border-slate-600 bg-slate-900 px-3 py-2 text-sm text-slate-100" placeholder="Slots">
            <button id="gs-settings-slots-save" type="button" class="ui-button ui-button--primary">Update slots</button>
        </div>
    </div>

    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <h4 class="text-xs font-semibold uppercase text-slate-300">Automation</h4>
        <div class="mt-3 grid gap-3 md:grid-cols-2">
            <div class="rounded-lg border border-slate-700 bg-slate-950/40 p-3 text-xs text-slate-200">
                <span class="mb-2 block font-semibold">Auto-Backup</span>
                <label class="flex items-center"><input id="gs-auto-backup-enabled" type="checkbox" class="mr-2"> Aktiv</label>
                <select id="gs-auto-backup-mode" class="mt-2 w-full rounded-lg border border-slate-600 bg-slate-900 px-2 py-1 text-xs text-slate-100">
                    <option value="manual">Manual</option>
                    <option value="auto">Auto</option>
                </select>
                <div id="gs-auto-backup-time-wrap" class="mt-2">
                    <label for="gs-auto-backup-time" class="mb-1 block text-[11px] uppercase tracking-wide text-slate-400">Time</label>
                    <select id="gs-auto-backup-time" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-2 py-1 text-xs text-slate-100"></select>
                </div>
            </div>

            <div class="rounded-lg border border-slate-700 bg-slate-950/40 p-3 text-xs text-slate-200">
                <span class="mb-2 block font-semibold">Auto-Restart</span>
                <label class="flex items-center"><input id="gs-auto-restart-enabled" type="checkbox" class="mr-2"> Aktiv</label>
                <div id="gs-auto-restart-time-wrap" class="mt-2">
                    <label for="gs-auto-restart-time" class="mb-1 block text-[11px] uppercase tracking-wide text-slate-400">Time</label>
                    <select id="gs-auto-restart-time" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-2 py-1 text-xs text-slate-100"></select>
                </div>
            </div>

            <div class="rounded-lg border border-slate-700 bg-slate-950/40 p-3 text-xs text-slate-200">
                <span class="mb-2 block font-semibold">Auto-Update</span>
                <label class="flex items-center"><input id="gs-auto-update-enabled" type="checkbox" class="mr-2"> Aktiv</label>
                <div id="gs-auto-update-time-wrap" class="mt-2">
                    <label for="gs-auto-update-time" class="mb-1 block text-[11px] uppercase tracking-wide text-slate-400">Time</label>
                    <select id="gs-auto-update-time" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-2 py-1 text-xs text-slate-100"></select>
                </div>
            </div>

            <label class="rounded-lg border border-slate-700 bg-slate-950/40 p-3 text-xs text-slate-200">
                <span class="mb-2 block font-semibold">Versionssperre</span>
                <input id="gs-version-lock-enabled" type="checkbox" class="mr-2"> Aktiv
                <select id="gs-version-lock-version" class="mt-2 w-full rounded-lg border border-slate-600 bg-slate-900 px-2 py-1 text-xs text-slate-100"></select>
            </label>
        </div>
        <div class="mt-3">
            <button id="gs-automation-save" type="button" class="ui-button ui-button--primary">Automation speichern</button>
        </div>
    </div>
</div>

<script src="{{ asset('js/gameserver/shared/domMount.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/errors.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/apiClient.js') }}"></script>
<script src="{{ asset('js/gameserver/settings-app.js') }}"></script>
