<div class="space-y-4">
    <div class="flex flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <div>
            <h2 class="text-lg font-semibold text-slate-900">{{ t('customer_instance_tab_files', page_locale()) }}</h2>
            <p class="text-sm text-slate-700">{{ t('customer_instance_files_tab_intro', page_locale()) }}</p>
        </div>
        <a href="{{ path('customer_instance_files', {id: instance.id}) }}" class="rounded-lg bg-indigo-600 px-4 py-2 text-xs font-semibold text-white hover:bg-indigo-700">
            {{ t('customer_instance_files_open_manager', page_locale()) }}
        </a>
    </div>

    <div class="rounded-lg border border-slate-200 bg-slate-50 p-4 text-sm text-slate-600">
        <p class="font-semibold text-slate-700">{{ t('customer_instance_files_tip_title', page_locale()) }}</p>
        <p class="mt-1">{{ t('customer_instance_files_tip_body', page_locale()) }}</p>
    </div>
</div>
