<div
    id="gameserver-console"
    class="space-y-4"
    data-url-command="{{ path('customer_instance_console_command_envelope', {id: instance.id}) }}"
    data-url-logs="{{ path('customer_instance_console_logs_envelope', {id: instance.id}) }}"
    data-url-health="{{ path('customer_instance_console_health_envelope', {id: instance.id}) }}"
    data-stream-url="{{ path('customer_instance_console_stream', {id: instance.id}) }}"
    data-stream-unavailable-message="{{ t('customer_instance_console_log_unavailable', page_locale()) }}"
    data-polling-active-message="{{ t('customer_instance_console_polling_active', page_locale()) }}"
    data-csrf-token="{{ csrf_token('instance_console_' ~ instance.id) }}"
>
    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <div class="flex flex-wrap items-center gap-2">
            <h3 class="text-sm font-semibold text-slate-100">{{ t('customer_instance_tab_console', page_locale()) }}</h3>
            <button type="button" id="gs-console-pause" class="ui-button ui-button--ghost ml-auto">Pause</button>
            <button type="button" id="gs-console-autoscroll" class="ui-button ui-button--ghost">Auto-scroll: On</button>
            <button type="button" id="gs-console-clear" class="ui-button ui-button--ghost">Clear</button>
        </div>
        <div id="gs-console-health" class="mt-3 rounded-md border border-slate-700 bg-slate-900/40 p-3 text-xs text-slate-300"></div>
        <div id="gs-console-error" class="hidden mt-3 rounded-lg border border-rose-300 bg-rose-50 p-3 text-sm text-rose-700"></div>
        <pre id="gs-console-log" class="gs-console-log-panel mt-3 overflow-y-auto overflow-x-auto rounded-md border border-slate-700 bg-slate-950 p-3 text-xs text-slate-200"></pre>
    </div>

    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <label for="gs-console-command" class="text-xs font-semibold uppercase text-slate-300">Command</label>
        <div class="mt-2 flex gap-2">
            <input id="gs-console-command" type="text" class="w-full rounded-lg border border-slate-600 bg-slate-900 px-3 py-2 text-sm text-slate-100" placeholder="status">
            <button type="button" id="gs-console-send" class="ui-button ui-button--primary">Send</button>
        </div>
    </div>
</div>

<script src="{{ asset('js/gameserver/shared/domMount.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/errors.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/apiClient.js') }}"></script>
<script src="{{ asset('js/gameserver/console-app.js') }}"></script>
