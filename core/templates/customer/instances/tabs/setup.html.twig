<div class="space-y-6">
    <div>
        <h2 class="text-lg font-semibold text-slate-900">Setup</h2>
        <p class="text-sm text-slate-700">Erforderliche Variablen und Secrets für deinen Gameserver.</p>
    </div>

    <div class="rounded-lg border border-slate-200 bg-slate-50 p-4">
        {% if setupStatus.is_ready %}
            <p class="text-sm text-slate-600">Alle Pflichtfelder sind gesetzt. Du kannst den Server installieren und starten.</p>
        {% else %}
            <p class="text-sm text-slate-600">Bitte trage die fehlenden Angaben ein, um den Server nutzen zu können.</p>
            <ul class="mt-2 list-disc pl-5 text-sm text-slate-600">
                {% for label in setupMissingLabels %}
                    <li>{{ label }}</li>
                {% else %}
                    <li>Einige Felder müssen vom Support ergänzt werden.</li>
                {% endfor %}
            </ul>
        {% endif %}
    </div>

    <div class="grid gap-6 lg:grid-cols-2">
        <div class="rounded-lg border border-slate-200 bg-white p-4">
            <p class="text-xs font-semibold uppercase text-slate-700">Variablen</p>
            {% if setupMessages.vars %}
                <div class="mt-3 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-2 text-sm text-emerald-700">{{ setupMessages.vars }}</div>
            {% endif %}
            <form action="{{ path('customer_instance_setup_vars', {id: instance.id}) }}" method="post" class="mt-4 space-y-4">
                {% for field in setupVars %}
                    <div>
                        <label class="block text-xs font-semibold text-slate-700 uppercase">
                            {{ field.label }}{% if field.required %} *{% endif %}
                        </label>
                        <input
                            name="vars[{{ field.key }}]"
                            value="{{ field.value }}"
                            type="{{ field.type == 'number' ? 'number' : 'text' }}"
                            class="mt-2 w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm"
                        >
                        {% if field.helptext %}
                            <p class="mt-1 text-xs text-slate-600">{{ field.helptext }}</p>
                        {% endif %}
                        {% if field.error %}
                            <p class="mt-1 text-xs text-rose-600">{{ field.error }}</p>
                        {% endif %}
                    </div>
                {% else %}
                    <p class="text-sm text-slate-500">Keine Variablen definiert.</p>
                {% endfor %}
                <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 text-xs font-semibold text-white">Variablen speichern</button>
            </form>
        </div>

        <div class="rounded-lg border border-slate-200 bg-white p-4">
            <p class="text-xs font-semibold uppercase text-slate-700">Secrets</p>
            {% if setupMessages.secrets %}
                <div class="mt-3 rounded-lg border border-emerald-200 bg-emerald-50 px-4 py-2 text-sm text-emerald-700">{{ setupMessages.secrets }}</div>
            {% endif %}
            <form action="{{ path('customer_instance_setup_secrets', {id: instance.id}) }}" method="post" class="mt-4 space-y-4">
                {% for field in setupSecrets %}
                    <div>
                        <label class="block text-xs font-semibold text-slate-700 uppercase">
                            {{ field.label }}{% if field.required %} *{% endif %}
                        </label>
                        <div class="mt-2 flex flex-col gap-2">
                            <input
                                name="secrets[{{ field.key }}]"
                                type="{{ field.type == 'number' ? 'number' : 'password' }}"
                                class="w-full rounded-lg border border-slate-200 bg-slate-50 px-3 py-2 text-sm"
                                placeholder="Neues Secret eingeben"
                            >
                            <span class="text-xs text-slate-600">Gesetzt: {{ field.is_set ? 'Ja' : 'Nein' }}</span>
                        </div>
                        {% if field.helptext %}
                            <p class="mt-1 text-xs text-slate-600">{{ field.helptext }}</p>
                        {% endif %}
                        {% if field.error %}
                            <p class="mt-1 text-xs text-rose-600">{{ field.error }}</p>
                        {% endif %}
                    </div>
                {% else %}
                    <p class="text-sm text-slate-500">Keine Secrets definiert.</p>
                {% endfor %}
                <button type="submit" class="rounded-lg bg-indigo-600 px-4 py-2 text-xs font-semibold text-white">Secrets speichern</button>
            </form>
        </div>
    </div>
</div>
