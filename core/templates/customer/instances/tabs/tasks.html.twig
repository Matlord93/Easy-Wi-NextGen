<div
    id="gameserver-tasks"
    class="space-y-4"
    data-url-tasks="{{ path('customer_instance_tasks_api_list', {id: instance.id}) }}"
    data-url-cancel-template="{{ path('customer_instance_tasks_api_cancel', {id: instance.id, taskId: '__TASK_ID__'}) }}"
    data-url-logs="{{ path('customer_instance_tasks_api_logs', {id: instance.id}) }}"
    data-url-health="{{ path('customer_instance_tasks_api_health', {id: instance.id}) }}"
>
    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <h3 class="text-sm font-semibold text-slate-100">{{ t('customer_instance_tab_tasks', page_locale()) }}</h3>
        <p class="mt-1 text-xs text-slate-300">{{ t('customer_instance_tasks_intro', page_locale()) }}</p>
        <div id="gs-tasks-error" class="hidden mt-3 rounded-lg border border-rose-300 bg-rose-50 p-3 text-sm text-rose-700"></div>

        <div class="dashboard-table-wrapper mt-4">
            <table class="dashboard-table">
                <thead>
                <tr>
                    <th>ID</th>
                    <th>Type</th>
                    <th>Status</th>
                    <th>Created</th>
                    <th>Actions</th>
                </tr>
                </thead>
                <tbody id="gs-tasks-list">
                <tr><td colspan="5" class="dashboard-table__empty">Loading tasks…</td></tr>
                </tbody>
            </table>
        </div>
    </div>


    <div class="rounded-lg border border-slate-700 bg-slate-900/40 p-4">
        <h4 class="text-xs font-semibold uppercase text-slate-300">Task logs</h4>
        <div id="gs-tasks-logs" data-empty="1" class="mt-3 h-56 overflow-auto rounded-md border border-slate-700 bg-slate-950 p-3 text-xs text-slate-400">Select a task to load logs.</div>
    </div>
</div>

<script src="{{ asset('js/gameserver/shared/domMount.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/errors.js') }}"></script>
<script src="{{ asset('js/gameserver/shared/apiClient.js') }}"></script>
<script src="{{ asset('js/gameserver/tasks-app.js') }}"></script>
