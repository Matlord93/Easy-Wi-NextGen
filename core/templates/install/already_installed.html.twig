{% extends 'install/base.html.twig' %}
{% trans_default_domain 'installer' %}

{% block title %}{{ 'already_installed.title'|trans }} · Easy-Wi NextGen{% endblock %}

{% block content %}
<div class="mx-auto max-w-4xl space-y-6">
    {% include 'install/_language_switcher.html.twig' with { step: step } %}

    <div class="rounded-xl border border-amber-200 bg-amber-50 px-6 py-5 text-amber-800">
        <h2 class="text-lg font-semibold">{{ 'already_installed.title'|trans }}</h2>
        <p class="mt-2 text-sm">{{ 'already_installed.lead'|trans }}</p>
        <a href="{{ loginUrl }}" class="mt-4 inline-flex items-center rounded-lg bg-amber-600 px-4 py-2 text-sm font-semibold text-white shadow-sm transition hover:bg-amber-500">
            {{ 'already_installed.login_link'|trans }}
        </a>
    </div>
</div>
{% endblock %}
