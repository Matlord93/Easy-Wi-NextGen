name: Combined Release (Optional)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Notice
        run: |
          echo "This workflow is optional."
          echo "Core/Agent/Installer workflows already upload release assets on tag pushes."
          echo "If you want a single workflow to aggregate everything, we can add a reusable workflow or a release orchestrator later."
