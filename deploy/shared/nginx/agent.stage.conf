server {
    listen 8087;
    server_name _;

    location = /healthz {
        add_header Content-Type text/plain;
        return 200 'agent:ok:stage';
    }

    location / {
        add_header Content-Type text/plain;
        return 200 'agent service (stage baseline)';
    }
}
