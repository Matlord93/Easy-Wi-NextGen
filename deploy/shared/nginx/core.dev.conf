server {
    listen 8080;
    server_name _;

    location = /healthz {
        add_header Content-Type text/plain;
        return 200 'core:ok:dev';
    }

    location / {
        add_header Content-Type text/plain;
        return 200 'core service (dev baseline)';
    }
}
